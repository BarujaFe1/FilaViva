# Public lab demo uses the static snapshot in /public/demo/snapshot.json by default.
# Set to 1 to force static mode even if a local API is reachable.
NEXT_PUBLIC_USE_STATIC_DEMO=1

# Optional live FastAPI (local or hosted). Leave unset for static-only public demo.
# NEXT_PUBLIC_API_BASE=http://localhost:8000
